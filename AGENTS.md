# AGENTS.md

本文件用于约束代理在本仓库中执行任务时的工作方式。

## 适用范围

- 作用于仓库根目录及其所有子目录。

## 基本要求

- 变更前先阅读相关代码和文档，避免基于猜测修改。
- 优先做最小必要改动，保持现有风格一致。
- 不要修改与当前任务无关的文件。
- 如发现用户已有未完成改动，不能直接覆盖或回退。

## 文档更新规则

- 只要任务涉及代码变更，在任务完成前必须同步更新 `docs/` 下的相关文档。
- 文档更新是交付的一部分，未更新 `docs/` 时，任务不能视为完成。
- 如果已有文档可以承接这次变更，直接更新对应文档。
- 如果现有文档都不适合，必须在 `docs/` 下新增文档说明本次变更。
- 文档内容至少应覆盖下面内容中的相关项：
  - 变更后的用法
  - 变更后的行为
  - 新增配置项或导出项
  - 兼容性、限制或迁移说明
- 如果文档入口发生变化，需同步更新 `README.md` 中的文档链接。

## 任务完成前检查

- 确认代码改动与任务目标一致。
- 确认 `docs/` 下文档已经同步更新。
- 确认文档描述与当前实现一致，不留过期说明。

