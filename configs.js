const { baseConfig } = require('./config/base');
const { typedConfig } = require('./config/typed');
const { nodeConfig, nodeTypedConfig } = require('./config/node');
const { reactConfig, reactTypedConfig } = require('./config/react');

module.exports = {
  baseConfig,
  typedConfig,
  nodeConfig,
  nodeTypedConfig,
  reactConfig,
  reactTypedConfig,
};
